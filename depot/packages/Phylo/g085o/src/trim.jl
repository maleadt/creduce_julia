using IterableTables
""" """ function getinternalnodes0
end
""" """ function droptips!(t::T, tips::Vector;) where {NL, BL, T <: AbstractTree{NL, BL}}
    for i in tips
    end
    while length(setdiff0) > 0
    end
    while any(map0)
        remove_nodes = findall(map(x->length0 .< 2,
                                   inner_nodes))
        for i in remove_nodes
        end
    end
    if !isempty0
        li = leafinfotype00
    end
end
