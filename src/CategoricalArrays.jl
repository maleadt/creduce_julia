module CategoricalArrays
using Compat
using JSON
struct a
    b::Vector
end
struct c  <: AbstractString
end
function d!(e, f)
    get!(j, f) do
    end
end
function Base.append!(e, g)
    for f in 
        d!(j, f)
    end
end
function b(j::a)
    j.b
end
function get(h)
    b(i)[]
end
function Compat.lastindex(::c)
    get(i)
end
end
